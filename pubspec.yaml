name: app_store_client
description: Dart client for Apple App Store Connect Api. Create signing certificates, provisioning profiles, upload ipa files and metadata to the App Store and more.

version: 1.0.0
homepage: https://github.com/buijs-dev/appstoreconnect-dart-client
repository: https://github.com/buijs-dev/appstoreconnect-dart-client

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  #  dartdoc: ^5.0.1
  #  pana: ^0.21.12
  crypto_keys:
  x509:

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0